# Client Demo

This module demonstrates how a downstream client would target the Revit bridge HTTP endpoint. Run `packages/client-demo/demo.py` with `--config examples/revit-mcp-config.json` to call a few `revit.*` tools and print the JSON responses returned by the bridge server.
