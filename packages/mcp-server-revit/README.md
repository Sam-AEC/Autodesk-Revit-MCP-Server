# MCP Server Revit Package

This package contains the Python MCP stdio server, schema definitions, bridge clients, tool handlers, and audit/security helpers described in the migration plan.

Use `python -m revit_mcp_server` to run the server after installing the package (`pip install -e packages/mcp-server-revit[dev]`).
